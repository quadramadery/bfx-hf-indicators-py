{
  "name": "bfx-hf-indicators-py",
  "version": "1.0.0",
  "description": "This repo contains a collection of trading indicators implemented to support incremental updates, both from trade and candle data (depending on the indicator)",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bfx-hf-indicators": "^1.0.2",
    "js-to-py": "^1.0.2"
  }
}
